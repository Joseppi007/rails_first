<%= form_with(model: fav_color) do |form| %>
  <% if fav_color.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fav_color.errors.count, "error") %> prohibited this fav_color from being saved:</h2>

      <ul>
        <% fav_color.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <tr>
      <td><%= form.label :first_name, style: "display: inline-block" %></td>
      <td><%= form.text_field :first_name %></td>
    </tr>

    <tr>
      <td><%= form.label :last_name, style: "display: inline-block" %></td>
      <td><%= form.text_field :last_name %></td>
    </tr>

    <tr>
      <td><%= form.label :color, style: "display: inline-block" %></td>
      <td><%= form.text_field :color %></td>
    </tr>
  </table>

  <%= form.number_field :user_id, id: :friend_user_id, value: current_user.id, type: :hidden %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
